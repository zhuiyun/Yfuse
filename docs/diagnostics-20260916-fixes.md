# 2026-09-16 夜间诊断修复

依据：`Yfuse-diagnostics-20260916-213553.zip`，北京时间 21:28–21:35:56，
设备 OPPO PLG110 / Android 16，日志版本 1.0.66、1.0.67。

## 播放

- 两次 MKV 起播均在 30 秒共享探测截止时间退出；读取 2 MiB 偏移发生三次尝试后超时。
  看门狗此前直接关闭 Cronet，绕过了适配传输内部的异常降级分支，下一次尝试仍可能选择 Cronet。
  现在看门狗在关闭前记录当前 Cronet 路由失败；后续重试及共享该路由的预取走 OkHttp。
  普通拖动、退出等主动取消不改变路由健康状态；保留现有字节范围、实体校验和总超时预算。
- 起播/定位的小范围读取不再仅首次使用小切片；尚未进入播放时，允许按实际偏移获取小切片，
  不等待未完成的整块预取。稳定播放仍使用整块缓存与预取。
- 降低预取并发时保留窗口内已经完成或至少下载 75% 的块，减少取消后重新下载。
- 第三个片源成功播放后发生两次缓冲，总计 4,921 ms，队列饥饿计数 9。
  连续独立卡顿逐次增加恢复储备，上限受目标队列时长约束；第一次起播仍保留低门槛。
  满队列和 EOF 仍可放行，拖动重置恢复策略。
- 解复用队列以逐轨有序时间戳计数维护缓冲跨度，避免每次入队及每次取样遍历全部包。
  保留重复时间戳、重排帧、退回样本与拖动后的正确边界。

## 同步、服务与诊断

- 已观看记录第二页失败后不再把截断结果当作完整快照返回；刷新失败保留上次完整状态，
  不触发基于缺失条目的冲突判断及写回。原有取消、鉴权冷却和失败退避继续生效。
- 启动进度拉取改为后台批量导入，每批统一排序、序列化和落盘；避免在主线程逐条重写全量记录。
  批量导入仍持有账号/档案协调锁，校验 scope token，跳过本地尚未上传的修改。
- 鉴权错误提示同时覆盖登录资料错误和已有登录失效；502/503/504/522/524 显示服务暂不可用提示。
- 更新清单落后时，手动检查显示明确错误，自动检查安静返回；已有下载、暂停和可安装包保留。
  不把落后清单显示为“已是最新版”，也不据此删除较新的下载记录。
- 合法的空 JSON 弹幕集合视为成功的空结果；错误对象和无法识别的正文仍为失败。
- 导出保留匿名 `serverref`（进程随机盐派生），用于关联同一服务器的请求，继续隐藏真实地址与账号。
  扩充有限的属性容量以保留完整 QoE；音画偏差未测得时区分无音轨、无音频时钟和视频时间戳不可信。

## 验证及边界

针对性回归覆盖 Cronet 打开/读取被看门狗终止、主动取消、范围与缓存读取、重复缓冲、
队列时间戳边界、分页失败、同步策略、鉴权冷却、空弹幕、更新状态保留和日志脱敏。
Android 共享模块编译及 255 项针对性测试通过（0 失败、0 跳过）。
最终取消处理补丁重新编译后，同步相关 25 项回归通过，包含新增的落盘时取消场景；共覆盖 256 个不同测试。

日志不能证明全部卡顿来自网络或全部来自客户端。实际片源、服务器凭证不在诊断包内，
本次不能复现真实 CDN/反代的 500/522 或确认服务器恢复，也未更改远端更新清单。
首页 534 ms 慢帧缺少调用栈，仍需真机帧分析定位；批量同步消除了一处已确认的主线程重负载，
但不能据此宣称首页所有慢帧已消除。
原记录 14.653 秒首帧及 50 帧丢弃需在新构建真机复测。
