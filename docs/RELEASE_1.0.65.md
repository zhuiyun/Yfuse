# 1.0.65（227）代码与本地交付记录

## 合并范围

本轮家庭资料、个人片库、数据同步、设备接力、Trakt、一起看多设备、字幕、下载和稳定性改动，已与实际交付 1.0.64（226）的源码提交 `8dc1e5b22fbd74f014670bf07c1773891f719191` 合并。该提交包含远程主分支 `52dba956bce04a6994f38452baed492c32b1572f` 的历史。

保留关闭页缩小收圆、仅上下氛围光、粒子反馈与 YCore 修复，同时合入本轮黑边探测和取色取消恢复。合并范围不代表仓库其他所有远程分支都已合入。

完整功能说明见根目录 `release-notes.txt`。设备接力、Trakt 和部分一起看改动需要配套服务端更新；此次没有部署服务端。Trakt 接入代码已完成，开发者应用尚未注册。

## 已验证的本地 APK

- 应用 ID：`com.yfuse`。
- 版本：1.0.65（227），接续实际上一包 1.0.64（226）。此前误标的本地 1.0.56（218）包已撤回。
- 类型：手机／平板 Full ARM64，含 YCore、MPV 与 MDK。
- 文件：`Yfuse-1.0.65-227-full-arm64-signed.apk`。
- 大小：29,481,327 字节。
- SHA256：`4450e7642a469ef9408f8aff0809e5a7617f73b513815f40a3aabbead4e9e0b4`。
- 正式证书 SHA256：`373e36d363965b6c1ae0a68c3db9537831d137ea6c38f094608bf243e7be3e84`。

## 验证范围

- 2,887 项 JVM／Android 单元测试通过：客户端 2,612，TV 63，服务端 204，协议 8；无失败、错误或跳过。
- 58 项 Python 脚本测试及五个模块 ktlint 检查通过。
- Release 构建、设计系统、版本、正式签名、播放运行时与生产 GPU 门禁通过。
- 实际 APK 的版本、签名、ZIP CRC、ARM64 库集合、YCore 字节和 ZIP／ELF 16 KB 对齐通过。
- 12 个对应原生库与实际上一交付 APK 逐字节一致。
- 按要求没有执行真实播放或安装设备。上传代码不等于发布 APK 或部署服务端。

## 原生制品与下载固定值

本地签名使用 workflow run `34701134934` 的 native artifact `10299938547`，制品 ZIP SHA256 为 `07d8eeedfa3a8feb3e4f6945105cd0f771ab29f9e918d3f7a2028bd90ba93f81`。其来源提交与上面的 1.0.64 基线一致。

本地安装按仓库脚本拆分重复 GPU 条目后，兼容 carrier SHA256 为 `db5f9f2f81d42457e2c6dfa84c6645ee4e140473c8a862a85699aaa02522c75a`。签名时依照签名 workflow 临时同步两个 MPV pin，验证与构建完成后，上传代码前将 `scripts/engine-checksums.sha256` 恢复为已发布下载制品的固定值，保证 `scripts/fetch-engines.sh` 的 URL 和哈希保持一致。

APK、原生二进制、凭据、本地缓存、原始诊断日志和源码备份保留在本机，不提交到 Git。此整理仅恢复构建时的下载固定值，不改变已验证的应用源码或已交付 APK。
