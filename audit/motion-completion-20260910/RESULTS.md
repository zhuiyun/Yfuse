# 动效与播放器交接完善 · 2026-09-10

本轮承接用户列出的 11 组剩余工作，并加入“从头播放提示减少干扰”的调整。代码、资源与本地回归已整理在本目录。没有连接手机，没有执行设备测试、8/24 小时内核门禁、上传代码或生成新签名 APK。

| 项目 | 本轮实现 | 关键边界 |
|---|---|---|
| 海报模糊揭示 | 共享 LRU 缓存最多 64 个 `BlurEffect`，按 0.5 物理像素分档，最大量化误差 0.25 像素 | 不缓存位图；零模糊返回空效果；内存命中、减弱动效与隐藏路由沿用即时展示 |
| 下一集倒计时 | 绘制阶段读取插值，每次播放采样最多预测 500 ms，按倍速计算 | 暂停、缓冲、取消、隐藏、减弱动效会停止预测；跳转立即校准；换集使用独立状态；自动播放仍由播放逻辑决定 |
| 播放器拆分 | 抽出网络恢复、投屏加载/队列同步、进度上报绑定、下一集覆盖层与控制状态投影 | 网络恢复状态仍按媒体/版本持有；投屏失败不暂停本地；进度上报保持一个 actor 的生命周期 |
| 高频更新隔离 | 控制层只观察播放开始边界及菜单所需状态；进度条和下一集卡片单独读取实时播放状态；一起看循环直接读取最新状态 | 手势仍取得真实位置；跳过设置面板保留真实当前位置；遥控预览保留未知时长下的非负跳转位置 |
| 动效配置与循环 | 归并海报、搜索、骨架、等待、氛围光淡化、播放器提示和贴纸/反馈等时长；骨架、等待和搜索自写循环在系统动画缩放为零时挂起，恢复后重新运行 | 不更改网络、播放同步、弹幕业务时钟与超时；已有轮播和贴纸的可见性/减弱动效门控保留；隐藏页的光球不保留动画时钟 |
| 骨架到内容交接 | 首页、媒体库首页/列表、详情、日历等使用按页面阶段触发的轻量交接；日历首载使用日列表轮廓骨架 | 刷新有内容时保持内容；不因列表数量变化重放整页动画；只保留一棵页面内容树；详情自己的首图揭示继续存在 |
| 播放器进出 | 增加平台窗口进出资源，使用相对位移并淡化；API 34+ 使用窗口转场 API，旧版本使用兼容方式 | 不增加视频截图或延迟释放；保留手机横屏/平板方向策略；PiP 与减弱动效/系统关闭动画时跳过效果 |
| 搜索交接 | 点击搜索按钮时记录一次数值坐标，输入框入场从按钮位置展开；请求中轻微吸合，结束时复原，保留局部扫光与结果交接；首批结果按 55 ms 分层 | 输入、分页、多服务器逐步完成、结果重排不重复播放入场；不保留 View、Context 或图片；关闭高亮/减弱透明度时禁用装饰 |
| 页面动效 | 日历筛选展开、日期箭头、稳定键列表位移、分段指示器、选中颜色过渡、服务器布局交接、一起看成员列表与就绪/复制状态交接 | 分段位置取实际测量边界，支持不同宽度和 RTL；成员以 clientId 为键；延迟数值更新不重复播放就绪交接 |
| 播放器反馈 | 双击快进/快退在点击位置显示短波纹；音量与一起看提示跟随控制栏进出；预览图短交叉淡化并借用上一帧的 Coil 缓存键作占位 | 不增加全拼图解码或额外位图缓存；波纹不处理触摸；隐藏/减弱动效关闭装饰 |
| 导航与覆盖层 | 覆盖层接入预测返回进度与取消回弹；栈导航补退场位移、预测返回缩放 | 取消手势不提交返回；减弱动效移除空间变化；原有对话框配置继续保留 |
| 旧代码 | 删除确认无调用的私有日历旧容器和始终不可达的私有侧栏分支及按钮 | 保留公开导航策略入口、偏好兼容及 TV 焦点；没有按关键词批量删除跨模块代码 |
| 续播提示 | “已续播 · 从头播放”改在进度条附近左侧显示，去掉亮色按钮，默认从 6 秒缩短到 3 秒 | 初始缓冲不消耗阅读时间；操作、隐藏控制、开面板、锁定、换集永久收起；一次播放会话只出现一次；遵从无障碍推荐停留时间 |

本地验证结果由 `test-counts.json` 和 `feature-tests.log` 记录。新增测试覆盖效果缓存容量与复用、量化误差、倒计时暂停/跳转/有限预测/换集、控制读取层重组隔离、内容阶段保持、搜索来源单次消费、续播提示计时及系统动画关闭后的循环休眠/恢复。

最终回归通过：共 **2,702** 项测试，失败、错误、跳过均为 0。手机端 2,460 项、TV 59 项、一起看协议 8 项、服务端 175 项；未受改动影响的任务使用 Gradle 增量结果。最终完整构建耗时 3 分 9 秒，宏基准模块编译通过。设计系统规范、Kotlin 格式检查和手机/TV 模块边界检查通过，`git diff --check` 无错误。提示淡出和换集期间增加了旧按钮点击保护。

`changed-files.txt` 是本轮文件清单；`source-snapshot.json` 保存每个文件相对本轮开始前的行数变化和 SHA-256。`before/` 只作为本次编辑前的本地备份，包含此前已完成但尚未打包的工作，不代表 Git HEAD。

真实设备上的帧耗时、耗电、方向切换时的 Surface 呈现、预测返回手势观感尚无本轮实测结论；本轮不把编译/单元测试当成设备体验验收。最新已有正式签名包仍为前一批的 `audit/releases/20260910-motion-optimization/Yfuse-1.0.51-signed-arm64.apk`，不包含本轮和前两轮未重新打包的改动。
