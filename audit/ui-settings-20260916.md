# UI 调整验证记录 · 2026-09-16

## 本次修改

- 库页默认恢复大海报轮播、分类与播放记录的原布局，移除新增的顶部门户/模式栏。
- 我的 → 更多外观与辅助 → 库页轮播图：默认开启，可持久保存关闭状态；关闭时保留服务器切换与全部服务器入口，并停止轮播计时。
- 详情页资源比较前移至操作区下方；显示加载、失败与重试；已解析的当前版本不再因搜索结果缺失而从比较中消失。
- 删除详情页个人清单标题，个人收藏与想看复用服务器操作按钮的尺寸、图标、颜色与选中态。
- 恢复原设置首页；我的内容合并收藏、想看与观看历史。家庭资料、同步状态、设备接力、Trakt 使用原设置页的标题、卡片、分隔线与行控件，各归入二级页。
- 移除重复的历史、账号、下载和设置入口；家庭资料与同步状态不再共用可以互相切换的个人中心标签栏。

## 验证

- Kotlin Android 编译通过。
- 7 个相关测试类，共 68 项测试，0 失败、0 错误、0 跳过。包含新增加的轮播偏好持久化，以及资源搜索遗漏/失败时保留当前版本的用例。
- 本次 19 个修改 Kotlin 文件的独立 ktlint 检查通过；git diff --check 通过。
- 测试结果：ui-settings-test-summary-20260916.json。
- 日志：ui-settings-compile-20260916.log、ui-settings-tests-20260916.log、ui-settings-lint-20260916.log。

## 验证边界

正常编译入口被已有本地 libmpv-release.aar 校验不一致阻挡：本地包及 sidecar 为 db5f9f2f81d42457e2c6dfa84c6645ee4e140473c8a862a85699aaa02522c75a，与项目固定允许列表不符。

代码验证命令显式排除了 :composeApp:verifyCustomMpvArtifact 和 :composeApp:verifyStandaloneYCoreArtifact，只验证 Kotlin 编译和单元测试；没有修改原生包、允许列表或构建校验代码，不代表原生包校验通过。

adb devices 未发现已连接设备，因此尚未执行真机视觉验收。未生成交付 APK，未改版本号，未提交或推送。
