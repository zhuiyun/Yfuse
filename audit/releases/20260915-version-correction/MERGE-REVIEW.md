# 已交付基线合并复核

实际上一交付包：1.0.64（226），源码提交 `8dc1e5b22fbd74f014670bf07c1773891f719191`。本地 HEAD 为 `04dc5086`，另有本轮未提交改动。`merge-baseline.py` 使用这三方输入完成合并，未重置或覆盖掉未提交工作。输入及备份见 `merge-inputs`、`before-merge`。

## 冲突处理

- `AmbientSamplingPolicy.kt`：快速取色需求只由上下黑边触发，保留内嵌黑边识别、两秒探测和 API 34 HDR helper。侧边黑边不产生光效需求，新增真实像素样本回归。
- `supply_chain_check.py`：合并已跟踪 lockfile 限定、多来源保留、共享版本固定、未锁定固定依赖纳入扫描、响应数量/类型/错误/分页完整性以及 advisory ID 校验。
- `test_supply_chain_check.py`：合并双方回归并清理重复导入；新增基线测试临时仓库跟踪锁文件，以匹配实际输入契约。
- 版本文件单独处理：新版本 1.0.65（227），保留 1.0.64 至之前历史，将误标 1.0.56 的本轮说明归入 1.0.65；误包和原说明保留并标记撤回。

## 播放页与氛围光静态复核

左上关闭锚点、等比缩小收圆、退出覆盖层与系统返回接线完整。正常关闭先暂停，由 composition 销毁释放引擎。家庭访问检查、切资料暂停退出、固定资料 owner 的历史与接力接线均保留。绘制仅保留上下光效，横向限制在画面宽度内。

## 六个界面文件静态复核

- `DetailScreen`：海报粒子与个人媒体操作同时保留。
- `HomeScreen`：与 226 基线一致，轮播粒子 effect 无重复。
- `LibraryHomeScreen`：粒子、全部服务器入口及紧凑/正常继续观看分支均保留；紧凑模式仍禁止自动轮播。
- `ProfileScreen`：个人首页/中心、家庭资料/历史/同步映射及粒子设置参数与弹窗齐全。
- `ProfileSettingsScreens`：与 226 基线一致，新增参数由调用方传入。
- `DownloadsScreen`：服务器权限过滤和家长设置门保留，下载状态粒子反馈合入。

未发现可确认的合并回归。上述为源码复核；运行检查另见 `verification.log` 与 `test-summary.json`。没有执行真实播放。
