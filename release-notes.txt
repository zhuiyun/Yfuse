1.0.62

• 修复点击播放页左上角关闭时画面停住一下才退出：不再在关闭前于主线程同步释放播放引擎，改为在页面销毁时释放，返回详情页立即完成。

1.0.61

• 修复播放页关闭动画：海报回退过渡改为绘制在控制栏与暂停画面之上，退出时画面与控件先同步压黑，再由海报缩回封面位置。
• 系统返回手势与关闭按钮统一走同一套海报回退过渡；封面启动的播放器在手势返回时不再无动画硬切。
• 窗口关闭动画默认保留淡出下移，仅在海报回退接管关闭时取消。

1.0.60

• 移除播放画面左右黑边的氛围光，保留上下效果，并限制上下光效不延伸至两侧。
• 修复退出播放页时海报被压扁或拉长的问题，过渡改为等比例缩放与居中裁剪。
• 仅存在左右黑边且控制栏隐藏时，不再为侧边光效采样画面。

1.0.59

• 修复启动页粒子预算的 Lint 类型识别，恢复检查通过后的正式签名构建。
• 修复音频释放与时钟回调竞争、直通回落后音画不同步、切音轨位置跳动及失败恢复。
• 修复播放结束后重播、路由事件误重建、重复自动下一集和 Enhanced 尾音截断。
• 修复远程文件替换后的缓存失效、Cronet 取消阻塞和代理断流后的媒体数据污染。
• 缓存目录不可写时继续网络播放，修复长时间播放时音频计数器回绕造成的时钟跳变。
• 移除未使用的算法查表资源以缩减安装包，保留更新签名校验。
• 兼容零时长 DASH/HLS 清单与部分硬解时间戳行为，修复无 Surface 暂停预览持续读流。
• 修复蓝光远程读取取消和原生渲染资源回收，补充 JNI 回调混淆保留规则。
• HTTP 播放、HTTP 许可证请求及跨协议播放重定向继续可用。
• 保留 1.0.58 的粒子样式、星尘启动动画和弹窗手势键盘修复。

1.0.58

• 修复弹窗与播放器面板上拉时误唤出软键盘：面板不再用滚动手势驱动输入法，仅保留为键盘让位。

1.0.57

• 粒子光效新增「风格」：星尘汇聚、星轨环流、流光丝带三种光粒形态，面板内实时小预览，与关闭/轻柔/增强档位独立保存。
• 启动页新增「光粒汇聚」编排：光粒从四周汇聚成标志，标志亮起后字标浮起。
• 详情页播放键按下时光粒向按压点聚拢；首页与媒体库轮播切页时在离开侧扫过一道汇聚光。
• 播放器一起看的新消息以聚拢光提示；房间信息里成员加入或离开时参与者列表有边缘扫光或消散反馈。

1.0.56

• 开放全部 43 款已实现弹窗动画，不再通过实验开关隐藏。
• 修复播放页从顶部下拉状态栏时误调音量或亮度；保留原有布局与正常区域手势。
• 新增关闭、轻柔、增强三档粒子光效与六类局部预览。
• 接入导航、弹窗、滑条、进度拖动、收藏、选中标记、按钮、遥控器焦点与当前首页手动轮播光感。
• 补齐下载状态、操作提示、缩略图预览和聊天未读反馈；增强档增加详情边缘、提示消散和启动 Logo 光点。
• 粒子使用固定容量与全局预算；减弱动态、页面不可见、后台或画中画时停用。

1.0.55

• 修复搜索完成时搜索框分裂后再合拢的视觉问题，保持输入框完整。
• 保留搜索加载反馈与结果过渡动画。

1.0.54

• 放大极光深色、浅色桌面图标的 Logo 主体，减少外围留白并修正视觉居中。
• 同步放大设置预览与应用内极光 Logo，保留原始图案与渐变颜色。

1.0.53

• 新增极光深色、极光浅色两款 Logo，可在“外观与辅助 → Logo 与开屏动画”中切换。
• 新 Logo 同步应用内标志与对应开屏，返回桌面后更新启动器图标。
• 版本接续上次正式签名包 1.0.52（214），升级为 1.0.53（215），同步项目版本配置。

1.0.45

• 修复推荐部分刷新失败时覆盖完整缓存、遗漏旧栏目并跳过后续自动重试的问题。
• 推荐加载区分网络、超时、认证、限流和数据异常，失败时保留已有内容并提示具体原因。
• 推荐缓存读取或清理异常不再中断在线加载，恢复连接后可重新获取完整推荐。

1.0.44

• 修复服务器管理切换时的异步状态覆盖，地址故障切换后使用最新地址刷新统计。
• 优化下载后台操作、整季批量加入、图片取色和外挂字幕取消，减少无效等待与界面线程负担。
• 修复 Exo 播放器初始化失败，以及 MPV 误报渲染失败、尚未出画面时提前结束加载的问题。
• 后台媒体探测中断后可重新尝试，减少缓冲或切集导致的探测遗漏。
• 新增信封拆启、星图织光、拼图扣合、沙漏汇流、风车开页五款弹窗动画，支持连续操作与减少动画设置。

1.0.32

• 修复内封字幕与外挂字幕处理中的 ASS 正则表达式，避免 Android ICU 初始化异常导致播放闪退。
• 新增 8 项字幕回归测试，并使用 ICU 对两处实际源码表达式执行编译和 16 项文本替换检查。
• 保留 1.0.31（193）的音频、账号和界面修复；本次不改变音视频解码与直连播放路径。
• 版本升级为 1.0.32（194），继续使用原正式签名，并校验安装包版本、签名及原生库来源。
• 字幕闪退修复不代替真机音视频验证，声音、首帧与杜比实际输出仍需在设备上复测。

1.0.31

• 合并 YCore 音频输出修复，继续收敛播放链路稳定性与音频设备兼容性。
• 统一项目弹窗与服务器页面弹窗的液态玻璃材质，降低高亮强度并统一视觉层级。
• 优化 PopupWindow / Modal 的展开与收缩动画，完善终止动作与退出动画的衔接。
• 优化底部 Tab 栏展开、收缩与内容切换动画，并完善大屏设备下的布局与搜索区域表现。
• 保持正式签名、版本、ABI、YCore 原生库来源与证书校验门禁，发布版本为 1.0.31（193）。

1.0.28

• 合并并收敛近期云端分支，修复 Android TV 复用 composeApp 源码后被 ktlint 重复检查、错误阻断正式构建的问题。
• 修复 YCore 启动、读前与恢复链路中的多处分支回归，减少起播阶段网络争用、异常重试和卡顿风险。
• 保持剧集版本选择按用户 HDR/Dolby 偏好决定，不再依赖媒体库入库顺序选择下一集版本。
• 加强 Emby/Jellyfin/Plex 数据与播放链路的回归保护，并保留直连播放、禁止以服务器转码掩盖客户端问题的策略。
• 整合 Android TV、Cast、播放队列、继续观看和诊断相关改动，重新执行正式签名、版本、ABI 与播放器原生库门禁。

1.0.24

• 修正 YCore 误判：启动期远程读前饥饿按网络故障恢复，不再错误惩罚解码器；网络恢复不再消耗纯自研内核的本地解码恢复次数。
• 提前 EOF 不再伪装成正常播完：使用真实音视频输出位置校验声明时长，有界重连后仍提前结束则报告网络中断，不自动跳集或写入终局学习样本。
• 修正音频-only/轨道元数据缺失时的解码器映射，音频解码器不再被当作视频轨道证据。
• 修复列表仍在惯性滑动时手动展开 Tab 栏又被连续滚动回调收起的问题；迁移 TV 入口代码到标准 KMP androidMain 源目录。

1.0.23

• 播放运行时切换为 YCore native-only：生产 APK 独立携带 FFmpeg、YCore GPU、libass、libbluray/libudfread，并对 ExoPlayer、mpv、MDK 运行组件执行打包门禁。
• 网络读取、FFmpeg 拆包、音视频解码与渲染分离执行；新增有界读前、码率感知水位、背压、启动缓冲及断流恢复迟滞，恢复前冻结音视频时钟。
• 完善 HLS/DASH/CMAF、自适应码率、Widevine/MediaDrm、持久缓存、HTTP Range、SMB/WebDAV 与蓝光原盘读取链路。
• 完善 ASS/SSA、PGS/VobSub、HDR10/HDR10+/HLG、Dolby Vision 与 Atmos 输出证据；未获得设备实际输出证据时不再按片源标签宣称双杜比。
• 加强非阻塞音频输出、音频设备切换、Surface 恢复、动态分辨率、丢帧与音画同步诊断。
• 追剧日历补充 TMDB/IMDb/TVDB 身份匹配、国产新剧发现与整季排期，并修正详情页和 Trickplay 数据链路。
• 播放切集、首帧和横竖屏重建改为连续画面衔接；增加实时网络/前向缓冲状态、响度均衡、一键音画同步及 OLED 长暂停保护。
• YCore 按实时吞吐量动态调整前向缓冲和恢复水位；启动后的非关键后台任务延迟到首帧之后，减少冷启动争用。
• 高级设置统一展示图片/视频缓存占用和权限健康状态；Plex/Emby/Jellyfin 按能力隐藏不支持的操作，诊断包增加有界播放事件时间线。
• 发布构建强制核对版本说明，CI 校验 APK 内部版本、签名、ABI 与播放器原生库；补充基线配置生成和首页滚动性能基准。

说明：实体设备的 Dolby Vision + Atmos、长播、功耗、温度、1,000 次 seek/Surface 重建及多芯片矩阵仍以应用内 YCore 验收报告的真实测量结果为准，不以代码存在或片源标签代替。
