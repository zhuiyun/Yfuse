<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <!--
      allowBackup is disabled in the manifest, so nothing here is reached on a compliant
      device. Excluding every domain outright (rather than naming the one preference file
      known to hold tokens) means a new store that happens to carry a credential is not
      transferred just because nobody remembered to list it.
    -->
    <cloud-backup disableIfNoEncryptionCapabilities="true">
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
        <exclude domain="device_protected" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
        <exclude domain="device_protected" path="." />
    </device-transfer>
</data-extraction-rules>
